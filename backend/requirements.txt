# Phase 1 — FastAPI foundation
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Phase 2 — PostgreSQL + ORM + migrations
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3
python-ulid==2.7.0

# Phase 3 — Auth
argon2-cffi==23.1.0
pyjwt==2.9.0
email-validator==2.2.0

# Phase 5 — Redis
redis==5.2.0

# Phase 6 — SSE for live log streaming
sse-starlette==2.1.3

# Phase 8 — AWS SQS
# aioboto3 transitively pins compatible boto3/botocore; pinning boto3
# separately causes a resolver conflict.
aioboto3==13.2.0

# Phase 10 — OpenTelemetry (auto-instrumentation + OTLP exporter)
# setuptools provides pkg_resources, which opentelemetry-instrumentation still
# imports; Python 3.13 slim images don't ship it by default.
setuptools>=70
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-asyncpg==0.48b0
opentelemetry-instrumentation-redis==0.48b0
opentelemetry-instrumentation-botocore==0.48b0
opentelemetry-instrumentation-logging==0.48b0
python-json-logger==2.0.7
